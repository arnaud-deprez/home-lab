apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base/cnpg-operator
  - ../base/traefik
patches:
  - path: traefik-values-patch.yaml
    target:
      kind: HelmRelease
      name: traefik
